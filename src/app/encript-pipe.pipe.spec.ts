import { EncriptPipePipe } from './encript-pipe.pipe';

describe('EncriptPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EncriptPipePipe();
    expect(pipe).toBeTruthy();
  });
});
